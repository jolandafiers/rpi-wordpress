# rpi-wordpress

# SSH to server
ssh pi@192.168.1.27

- `ls` = list directory
- `cd <folder name>` = go into folder
- `mkdir` <new folder name> = create new folder
- `mv <name> <destination folder>` move file into folder
